this contains the creation of a function that will auto-preform the calculation of the prices.

still accidently deleted something, time to un-do that and make it so that it's one commit that can be un-done